{
  "name": "retrox-frontend",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Lint config for RetroX's vanilla-JS frontend. Backend code lives under /backend with its own pyproject.toml; this package.json exists solely to wire ESLint up for the JS in this directory.",
  "scripts": {
    "lint": "eslint js"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^15.14.0"
  }
}
