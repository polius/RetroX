"""RetroX backend package.

The version literal here is the single source of truth — main.py and the
auth /me handler both read it via `from . import __version__`. Bump this
on a release; nothing else needs touching.
"""

__version__ = "2.1.0"
