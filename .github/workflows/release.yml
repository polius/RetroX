name: Release

on:
  release:
    types: [published]

jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: poliuscorp
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: poliuscorp/retrox

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set tag without first two chars
        id: tag
        run: |
          SHORT_TAG="${GITHUB_REF_NAME:2}"
          echo "SHORT_TAG=$SHORT_TAG" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            poliuscorp/retrox:latest 
            poliuscorp/retrox:${{ steps.tag.outputs.short_tag }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: index.docker.io/poliuscorp/retrox
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
